#include "flag.h"

//stdlib
//stdio 

// 
const __u_char flagcode(const int argc, const char* argv[]){

    __int16_t nflag;
    for(nflag = 0; nflag < argc; nflag ++){
        if(strcmp(argv[0], argv[0])){}
    }
    
}